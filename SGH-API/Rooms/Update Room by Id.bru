meta {
  name: Update Room by Id
  type: http
  seq: 4
}

patch {
  url: {{URL}}/rooms/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN-ADMIN}}
}

body:json {
  {
    "state": "Ocupado"
  }
}
