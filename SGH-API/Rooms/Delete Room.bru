meta {
  name: Delete Room
  type: http
  seq: 5
}

delete {
  url: {{URL}}/rooms/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN-ADMIN}}
}

body:json {
  {
    "state": "Ocupado"
  }
}
